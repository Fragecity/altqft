    
\documentclass[reprint, aps, prl, superscriptaddress]{revtex4-2}
\usepackage{graphicx, float, xcolor}
\graphicspath{{fig/}}
\usepackage[ruled,linesnumbered]{algorithm2e}
\usepackage{physics}
\usepackage{hyperref}
\usepackage{amsthm, amssymb, bm}
\usepackage[T1]{fontenc}

\let\originalsection\section

\renewcommand{\section}[1]{\par\medskip\noindent\textbf{{\textit{#1.}---}}}
\newcommand{\ii}{\mathrm{i}}
\newcommand{\myqc}{\text{HP}}
\newcommand{\uqc}[1]{U_{\mathrm{HP}}^{(#1)}}
\newcommand{\cpgate}[3]{\mathrm{CP}_{#1, #2}(#3)}
\newcommand{\fii}{\mathrm{DFI}}

\newcommand{\bkm}[1]{\textcolor{violet}{#1}}

\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem*{restatedtheorem}{Theorem}
\newtheorem*{restatedproposition}{Proposition}



\begin{document}
\title{$\mathcal{O}(n)$ alternative to Quantum Fourier Transform with efficient neural net classical post-processing}


\author{Kaiming Bian}%12231295@mail.sustech.edu.cn
\thanks{These two authors contribute equally.}
\affiliation{Shenzhen Institute for Quantum Science and Engineering, Southern University of Science and Technology, Shenzhen, 518055, China}
\affiliation{International Quantum Academy, Shenzhen, 518048, China}

\author{Zujin Wen}%zujinwen2-c@my.cityu.edu.hk
\thanks{These two authors contribute equally.}
\affiliation{Department of Physics, City University of Hong Kong, Tat Chee Avenue, Kowloon, Hong Kong SAR, China}
\author{Oscar Dahlsten}
\affiliation{Department of Physics, City University of Hong Kong, Tat Chee Avenue, Kowloon, Hong Kong SAR, China}
\date{\today}

\begin{abstract}
The Quantum Fourier Transform (QFT) is required by hidden subgroup algorithms including Shor's algorithm for factoring. The circuit depth and long-range gates of the QFT remains challenging for near-term hardware. To find more shall alternatives we identify two functional properties that are exploited by the QFT to enable HSP. Firstly the shift invariance property of the QFT allows for the removal of a random overall shift. Secondly, the QFT retains information about the hidden subgroup generator accessible in the computational basis. We quantify that information via the discrete Fisher information. We construct a family of shallow circuits using Hadamards and controlled-Phase gates, HP-L circuits, that preserve shift invariance. Numerical analysis shows these circuits retain Fisher information about the generator with the same scaling as the QFT.  Numerical experiments with the $\mathcal{O}(n)$ HP-1 replacing the $\mathcal{O}(n^2)$ QFT demonstrates successful Shor factoring, using an efficiently scaling neural network for classical post-processing. 
\end{abstract}

\maketitle


\section{Introduction} The $\mathcal{O}(n^2)$ circuit depth required by the standard Quantum Fourier Transform (QFT) presents experimental challenges. The QFT is used in the exponential quantum speedups of landmark protocols—such as Shor's algorithms for prime factorization and discrete logarithms \cite{Shor1994, Shor1997}, as well as Simon's algorithm \cite{Simon1997}. The associated family of algorithms is known as the (quantum) hidden subgroup problem (HSP) algorithms \cite{Kitaev1995, Jozsa2001, Lomont2004}. 
The $\mathcal{O}(n^{2})$ depth complexity of the QFT remains a critical bottleneck for implementing HSP algorithms in hardware. For a $100$-qubit system ($n=100$), the QFT demands thousands of circuit layers, which vastly exceeds current hardware capabilities. For instance, IBM's $127$-qubit Eagle processor is limited to a depth of $60$ layers even with extensive error mitigation \cite{kim2023evidence}, and Google's $105$-qubit Willow processor reaches its operational threshold at around $40$ layers~\cite{google2025quantum}. 
Quantum error mitigation protocols improve the performance, but with a sampling overhead that scales exponentially with the circuit depth~\cite{takagi2022fundamental, takagi2023universal}.




Strategies to mitigate the circuit depth required by the QFT have primarily focused on reducing hardware execution costs, often by only approximating the QFT or by shifting part of the computation to classical post-processing. These efforts have enabled record experimental QFT fidelities \cite{aumann2026demonstrating} through techniques including (i) truncated controlled rotations \cite{Coppersmith2002}, (ii) optimized CNOT routing tailored for restricted hardware topologies \cite{park2023reducing}, which approximates the QFT with \(\mathcal{O}(n\log n)\) depth, (iii) dynamic mid-circuit measurements \cite{baumer2024quantum}, and (iv) quantum circuit cutting, which trades coherent quantum depth for classical post-processing resources \cite{tang2021cutqc, Peng2020}. Despite these advances, approximation-based methods still retain \(\mathcal{O}(n\log n)\) depth in representative constructions, while circuit-cutting-based protocols shift the complexity to the classical domain and can require exponential classical resources \cite{tang2021cutqc, Peng2020}.


We here accordingly seek to design more  shallow alternatives to the QFT without introducing prohibitive classical post-processing burdens. Rather than approximate the QFT we take the approach of identifying key features of the QFT that make it compatible with the HSP. WE focus on shift-invariance combined with retention of information about the generator in question. Fig.~\ref{fig:illus}(a) summarizes the main idea.


We construct a shallow circuit family, called \myqc~circuits, that preserves shift invariance,  whilst retaining information about the generator. These circuits interleave transversal Hadamard (H) gates with $L$ layers of phase (P) blocks. The circuits are shift-invariant and numerical scaling analysis demonstrates exponentially increasing retention of Fisher information $\sim e^{kn}$, albeit with a worse exponent $k$ than QFT. That $\sim e^{kn}$ implies efficient scaling of the number of experimental runs required. 

We numerically simulate replacing the QFT in Shor's algorithm with \myqc-1 circuits, and using an efficiently scaling neural network~\cite{zaheer2017deep} for classical post-processing. Factorisation is successfully achieved.  

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\columnwidth]{illus v1.4.pdf}
    \caption{\label{fig:illus} \textbf{Shallow shift-invariant alternative to the QFT.} (a) Shift invariance is a critical property of the QFT. We identify a circuit family, denoted as \myqc-$L$, that inherently preserves the property. Within this broader family, \myqc-1 specifically serves as shallow-depth gates that are friendly to experiment. (b) In the proposed HSP algorithmic routine, the deep QFT block (the left gray gate) is directly replaced by the \myqc~circuit. A neural network then process the measurement outcomes, bypassing the standard continued fraction expansion (CFE) typically used in standard HSP. The above figure illustrates the HP circuit, which composed by Hadamard gates and phase gates. }
\end{figure}

\section{Necessary condition for Shift Invariance}
The standard quantum HSP algorithm is illustrated in Fig.~\ref{fig:illus}(b) with gray blocks \cite{Shor1994, Shor1997, Simon1997, Jozsa2001}.
After $U_f$, the state is proportional to $\sum_x \ket{x}\ket{f(x)}$. The $x$'s are elements of a finite abelian group $G$ with group addition +. We are guaranteed that $f(x+r)=f(x)$ for the unknown generator $r$ that we wish to find. In other words, the function f(.) is constant on cosets of $V$, the subgroup generate by $r$. Upon measuring (or imagining) measuring the second register to find $\ket{f(c)}$ the state becomes proportional to $\sum_{n\in 0,1,2...} \ket{c+nr}\ket{f(c)}$ with the shift $c$ acting as undesired noise obfuscating $r$ (additional details about the HSP algorithm are provided in
Appendix~\ref{append: hsp and fs}).

We now define shift invariance for HSP sampling and derive a necessary condition that any shift-invariant unitary must satisfy. For a subgroup $V \le G$, we denote the uniform superposition over its elements as $\ket{V} = \frac{1}{\sqrt{|V|}} \sum_{v \in V} \ket{v}$.

\begin{definition}[Shift invariance]
    % Let $T_c\ket{v} = \ket{v + c}$ be the shift operator for $c \in G$. 
    A unitary $U$ acting on the group register is shift-invariant for HSP sampling if
    \begin{equation}
    \label{eq:shift invariant def}
        \left|\bra{x}U\ket{V}\right|^2
        =
        \left|\bra{x}U \ket{c+V}\right|^2,
        \quad
        \forall x,c \in G,\ \forall V \le G.
    \end{equation}
\end{definition}

To identify shift-invariant circuits beyond the QFT, we first establish a necessary condition for shift invariance.
\begin{proposition}[Necessary condition of shift invariance]
\label{thm:necessary_modulus_condition}
    Suppose $U$ is a unitary on the group register that satisfies the shift-invariance condition above. Then, for every $x,y\in G$ there exist phases $\theta_{xy}\in\mathbb{R}$ such that
%    \begin{equation}
        %\left|\bra{x}U\ket{y}\right|=\frac{1}{\sqrt{|G|}}.
    %\end{equation}
    \begin{equation}
        \bra{x}U\ket{y}=\frac{1}{\sqrt{|G|}}e^{\ii\theta_{xy}}.
    \end{equation}
\end{proposition}

\begin{proof}[Proof sketch of Proposition~\ref{thm:necessary_modulus_condition}]
The proof hinges on analyzing the shift-invariance condition for the trivial subgroup $V = \{0\}$. For subgroup $V = \{0\}$, the state $\ket{V}$ is the computational basis state $\ket{0}$, and the shifted state is $\ket{c+V} = \ket{c}$. The requirement (Eq.\eqref{eq:shift invariant def}) that $\left|\bra{x}U\ket{0}\right|^2 = \left|\bra{x}U\ket{c}\right|^2$ for all $x, c \in G$ implies that, for any fixed output $x$, the magnitude of every matrix element in the $x$-th row of $U$ must be identical. The special case induce the uniform magnitude $\bra{x}U\ket{y} = 1/\sqrt{|G|} e^{\ii \theta_{xy}}$. The full details of this argument are provided in Appendix~\ref{app:necessary condition of sinv}.
\end{proof}

\section{L-phase-block Hadamard-Phase circuits (\myqc): A shift invariant circuit family}
Proposition \ref{thm:necessary_modulus_condition} suggests a simple design principle for constructing shift-invariant circuits. The condition \(|\langle x|U|y\rangle|=1/\sqrt{|G|}\) requires flat amplitude magnitudes, which are naturally generated by Hadamard gates. The remaining freedom lies in the relative phases \(\theta_{xy}\), which can be adjusted by controlled phase gates ($\mathrm{CP}(\theta)=\ket{00}\bra{00}+\ket{01}\bra{01}+\ket{10}\bra{10}+\exp(\ii\theta)\ket{11}\bra{11}$) without changing these magnitudes. This motivates the Hadamard-phase (HP) circuit family we shall now introduce. As illustrated in the upper subfigure of Fig.~\ref{fig:illus} (b), the \myqc-$L$ circuit is structured as an alternating sequence of Hadamard gates and phase blocks. To respect shift invariance (see Theorem \ref{thm:hp_circuits_are_sinv} below) we enforce three key rules (i) each qubit is acted on by a Hadamard gate exactly once, and (ii) each qubit pair is connected by at most one controlled-phase gate, (iii) each controlled phase gate must come together with 2 Hadamards, one before and one after the phase gate. Here, ``once'' refers to the entire $L$-layer circuit rather than to a single layer. Examples of valid circuits are illustrated in Fig.\ref{fig:illus}


To express the allowed circuits mathematically, we define two types of sets of qubits.
Let \(H_i\) denote the Hadamard gate applied to qubit
\(i\in\{1,2,\ldots,n\}\). For each layer \(l\), let
\(\Lambda_l\subseteq\{1,2,\ldots,n\}\) denote the set of qubits on which
Hadamard gates are applied in that layer. The qubits that are still allowed in terms of having a Hadamard on them in layer s after layer $l$, i.e.\ those that have not had a Hadamard on them in or before layer $l$, are denoted as the set \(\bar{\Lambda}_{\leq l}\).

In-between the Hadamard layers the two sets are coupled via a controlled-phase block, denoted as $\cpgate{\Lambda_l}{\bar{\Lambda}_l}{\bm{\theta}_{\Lambda_l\bar{\Lambda}_l}}$, which applies pairwise operations $\mathrm{CP}_{jk}(\theta_{jk})$ between each qubit $j \in \Lambda_l$ and $k \in \bar{\Lambda}_l$. Building upon those notations, we formally define the circuit architecture:
\begin{definition}[\myqc-$L$ circuit]
    A quantum circuit is classified as an \myqc-L circuit if it is constructed using the following layer-wise architecture
    \begin{equation}
        \uqc{L}({\bm{\theta}}) = \bigotimes_{i \in \Lambda_L} H_{i}\prod_{l=L-1}^{1} \left( \cpgate{\Lambda_l}{\bar{\Lambda}_l}{\bm{\theta}_{\Lambda_l\bar{\Lambda}_l}} \bigotimes_{i \in \Lambda_l} H_{i} \right),
    \end{equation}
    where the controlled-phase block is given by
    \begin{equation}
        \cpgate{\Lambda_l}{\bar{\Lambda}_l}{\bm{\theta}_{\Lambda_l\bar{\Lambda}_l}} = \prod_{j \in \bar{\Lambda}_l} \prod_{k \in \Lambda_l} \mathrm{CP}_{jk}(\theta_{jk}).
    \end{equation}
    The parameter set $\bm{\theta}_{\Lambda_l\bar{\Lambda}_l}$ is the collection $\theta_{jk}$, $j\in \Lambda_l$, $k \in \bar{\Lambda}_l$. 
\end{definition}


    
In the following, we show that HP circuits preserves shift invariance. This implies that such invariance is not unique to the QFT, but is a feature that arises in a wider family of quantum circuits. 


\begin{theorem}
\label{thm:hp_circuits_are_sinv}
    \myqc-$L$ circuits are shift invariant for $\mathbb{Z}_{2^{n}}$.
\end{theorem}
The proof is showed in Appendix~\ref{app:HPk are sinv}.

\section{\myqc-0 can be used in HSP over $\mathbb{Z}_{2^n}$}
We gives an analytical solution for HSP over $\mathbb{Z}_{2^n}$ via transversal Hadamard gates. We consider the cyclic group $\mathbb{Z}_{2^n}$ since the group can be naturally represented by computational basis. In this case, each subgroup is generated by a power of two,
\begin{equation}
    V=\{q2^p:q=0,\ldots,2^{n-p}-1\} \leq \mathbb{Z}_{2^n},
\label{eq: hp0 subgroup}
\end{equation}
where \(p\) specifies the subgroup generator \(2^p\). Applying a transversal Hadamard layer to a coset state gives
\begin{equation}
    H^{\otimes n}\ket{c+V}
    \propto
    \sum_{k\in\{0,1\}^{p}}(-1)^{k\cdot c}\ket{0}^{\otimes(n-p)}\ket{k}.
    \label{eq: hp0 after th}
\end{equation}
By measuring the state $H^{\otimes n}\ket{c+V}$, one observes that the first $n-p$ bits are always fixed to 0, while the remaining $p$ bits are uniformly distributed over $\{0,1\}^p$. Thus, the value of $p$ can be determined by identifying which bit positions are consistently zero. The detailed calculation and sampling algorithm are provided in Appendix~\ref{app:transversal_hadamard_sampling}.



\section{\myqc-$L$ for HSP over $\mathbb{Z}$: information on generator is retained}The protocol $\myqc$-0 fails for general cyclic groups $\mathbb{Z}_q$ because the measurement distributions corresponding to different subgroups $V$ are not distinguishable. The no distinguishability demonstrates that shift invariance alone is insufficient for successful hidden subgroup recovery, and the distinguishability should be the second condition. 

\begin{figure}[t]
    \includegraphics[width=0.45  \textwidth]{fi_fig v2.pdf}
    \caption{\label{fig:fi_scaling} 
    \textbf{Average discrete Fisher information (DFI) scaling with the number of qubits.} Results are shown for the QFT reference and several HP-family circuit settings. Solid curves show the mean, with shaded bands indicating mean $\pm$ one standard deviation.}


    
\end{figure}


Rigorously, we use the discrete Fisher information (DFI) as a metric of the distinguishability.
\begin{equation}
\label{eq:DFI}
    \fii(r, n) = \sum_{x = 0}^{2^n -1} \frac{\left(\text{Pr}(x\mid r+1) - \text{Pr}(x\mid r)\right)^2}{\text{Pr}(x\mid r)},
\end{equation}
where the $x$ represent measurement outcome and $r$ is the hidden subgroup generator, and $\text{Pr}(x\mid r)$ is the probability of measuring $x$ when the subgroup generator is $r$. The sample complexity for estimating a discrete parameter is formally governed by the DFI (Eq.\eqref{eq:DFI}) via the Hammersley-Chapman-Robbins bound~\cite{chapman1951minimum, hammersley1950estimating}, 
\begin{equation}
    m \ge \frac{\ln\left( 1 + \epsilon^{-2} \right)}{\ln\left( 1 + \fii(r) \right)},
\end{equation}
where $m$ is the required number of samples. The bound guarantees that retaining an exponentially growing DFI results in tractable sample complexity for the subsequent neural-network-based post processing. 


For standard QFT, we can express the DFI as
\begin{equation}
\label{eq:qft dfi}
    \fii_{\mathrm{QFT}}(r,n) \approx \frac{4\pi^2}{9}\left(\frac{2^{2n}}{r^2}-1\right) \sim \mathcal{O}\left(2^{2n}\right).
\end{equation}
The calculation of Eq.~\eqref{eq:qft dfi} is provided in Appendix~\ref{app:qft_fisher_information}. 
The Eq.~\eqref{eq:qft dfi} indicates that generator $r$ becomes easier to recover as the qubits number increases, which motivates us to limit $r$ with $1 \ll r \ll 2^n$. 

For \myqc{} circuits, the DFI is evaluated numerically. Specifically, for a fixed $r$, the circuit maps the periodic input components $\{\ket{qr}\}_q$ to a computational-basis distribution over outcomes $\ket{b}$, with the exact probabilities given by $\mathrm{Pr}(\ket{b}) = \left|\sum_q \langle b|U|qr\rangle\right|^2$.
To evaluate the worst-case parameter sensitivity as the system scales, Fig.~\ref{fig:fi_scaling} illustrates the minimum DFI against the qubit number $n$. This worst-case metric $\fii_{\min}(n)$ is formally defined as $\min_{1 \leq r \leq \lfloor 2^{n/2} \rfloor} \fii(r,n)$.
We bound the periods to $1 \leq r \leq \lfloor 2^{n/2} \rfloor$ to satisfy the condition $r \ll 2^n$.

Fig.~\ref{fig:fi_scaling} also compares the scaling of different \myqc circuits configurations, where solid curves and shaded bands represent the mean and standard deviation of $\fii_{\min}(n)$. The fixed-phase \myqc-1 circuit utilizes the skeleton, $\Lambda_1 = \{1,3,5, \cdots\}$ and $\Lambda_2 = \{2,4,6, \cdots\}$, with controlled-phase gates applying control phase gate with phase $\frac{2\pi}{2^{|i-j|}}$ between qubits $i$ and $j$. The optimized \myqc-1 circuit adopts the same skeleton but treats the gate phases as variational parameters, trained to maximize the $\fii_{\min}$. The deeper \myqc{}-$L$ circuits are constructed by generating valid random skeletons $\Lambda$. 


The proposed \myqc circuit family, particularly the shallow \myqc-1 configuration, reduces quantum depth requirements while preserving an exponential growth of DFI. As detailed in Appendix~\ref{app:fi_log_linear_growth}, numerical data for both the \myqc{} circuits and the QFT benchmark fit the log-linear model $\ln \fii_{\min}(n) = kn + b$. 




\begin{figure}[!t]
    \includegraphics[width=0.5\textwidth]{fig/recover v2.pdf}
    \centering
    % \caption{\label{fig:recovery_pipeline} Classical post-processing pipeline for subgroup recovery from \myqc-1 measurement data. The measured bit strings produced after the shallow quantum circuit are passed through a shared feature map and then ranked by a beam-search decoder to infer the candidate period. The figure illustrates that the measurement outcomes still retain period-dependent structure after the HP transformation, which can be extracted by an efficient neural post-processing stage rather than by an explicit QFT inversion.}
    \caption{\label{fig:recovery_pipeline}
    \textbf{Numerical simulation shows successful Shor factoring via HP-L and neural net post-processing.}
    (a) Training curves for 9-, 10-, and 11-qubit instances show rapid improvement in recovery accuracy.
    (b) Shor-type factorization instances colored by the minimum number \(k\) of HP-\(1\)-based guesses needed for successful period recovery. Most instances are solved with \(k=1\), while only a few require \(k=2\) or \(k=3\), showing the broad effectiveness of HP-\(1\)-based recovery.
}
\end{figure}

\section{Efficient Neural network for classical post-processing}
To bypass the exponential overhead of evaluating full probability distributions, we employ a polynomial-scaling Deep Sets neural network \cite{zaheer2017deep, edwards2016towards} to predict the hidden subgroup generator directly from the raw measurement outcomes $\{b_i\}$. The input data is generated using the optimized \myqc{}-1 circuit evaluated in Fig.~\ref{fig:fi_scaling}. These outcomes are fed into a shared feature map \(\phi\), which maps each bitstring \(b_i\) to a feature vector. The feature vectors are then summed to form an aggregated, permutation-invariant representation $\sum_i\phi(b_i)$. Subsequently, a beam-search decoder \cite{sutskever2014sequence, wiseman2016sequence} performs a heuristic top-$k$ search to rank candidate periods. By maintaining a restricted set of probable solutions, this architecture avoids exponential sample-space computations and outputs a ranked list of candidate generators for classical verification.

\section{Replacing \texorpdfstring{$\mathcal{O}(n^2)$}{O(n2)} QFT with \texorpdfstring{$\mathcal{O}(n)$}{O(n)} \myqc{}-1 in Shor's Algorithm}
We evaluate the optimized \myqc{}-1 circuit on Shor's algorithm, demonstrating that its shallow circuit measurements retain learnable features for successful classical post processing. Shor's Algorithm seeks the generator $r$ satisfying $a^r \equiv 1 \pmod{N}$. We select the base $a$ such that $1 \le r \le 2^{n/2}$, ensuring resolvability by the $n$-qubit register. As shown in Fig.~\ref{fig:recovery_pipeline}(a), the neural network achieves rapid convergence on 9-, 10-, and 11-qubit instances. Both the top-1 (correct period ranked first) and top-3 accuracies increase swiftly within a few epochs.

Our evaluation on Shor factoring demonstrates that our protocol can find the subgroup generator. As illustrated in Fig.~\ref{fig:recovery_pipeline}(b), each composite integer is colored by the minimum number of guesses $k$. Most instances are solved on the first attempt ($k=1$), indicating the raw measurement distribution provides sufficient information for period extraction. 

Our evaluation under a post-circuit global depolarizing channel demonstrates that the trained \myqc{}-1 neural decoder is robust against moderate environmental noise. Parameterizing the noise strength by $\eta$, the held-out recovery accuracy remains stable up to $\eta \approx 0.215$. Performance degrades at higher noise levels, dropping to $0.491$ for $9$ qubits and $0.881$ for $10$ qubits at $\eta=0.464$. At the fully depolarizing limit ($\eta=1$), the subgroup information is erased, reducing the model to chance-level guessing. Appendix~\ref{app:noise_robustness_details} provides detailed settings, complete numerical results, and corresponding interpretations.

\section{Summary and outlook}
We propose a hardware-efficient framework for solving the HSP by shallow, shift-invariant \myqc{}-1 circuits with a polynomial-scaling neural network. To verify the scalability of this protocol, we analyzed the growth model of the DFI. Finally, we evaluated our protocol on Shor's algorithm. The numerical simulations demonstrate that our approach successfully recovers the hidden subgroup with high accuracy, while remaining robust against moderate depolarizing noise.

Realizing a practical HSP protocol requires an optimized oracle $U_f$ implementations. Current strategies to mitigate the cost of $U_f$ include $\mathcal{O}(1)$-query classical pre-training of diagonal Hamiltonians \cite{learning_hamiltonian_2025}, space-time tradeoffs for non-Clifford gate reductions \cite{yu2025back}, and automatic oracle synthesis frameworks \cite{aos_qdk_2024}. 


Apart from the $\mathcal{O}(n)$ scaling being hardware friendly,  the grouped phase gates in \myqc circuits reduces are easier to achieve compared to individual phase gate execution. This hardware-level efficiency arises because consecutive phase operations can be merged into single continuous pulses across multiple experimental platforms, including in linear optics \cite{carolan2015universal, Kok2007}, neutral-atom systems \cite{saffman2010quantum, evered2023high, mohan2025parametrized}, trapped ions \cite{Leibfried2003, Tan2013}, and NMR \cite{vandersypen2004nmr}.

\section{Acknowledgements}
We acknowledge support from The Guangdong Provincial Quantum Science Strategic Initiative (Grant No. GDZX2503001) and the City University of Hong Kong (Proj. 9610623)
\section{Code Availability}
All code relevant to this work is publicly available in the GitHub repository: \url{https://github.com/Fragecity/altqft}.

\bibliography{ref.bib}
\input{appendix.tex}

\end{document}

9240521

